from django.apps import AppConfig


class BillingMorningConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.billing_morning"
    verbose_name = "Billing (Morning)"
